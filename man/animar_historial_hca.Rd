% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_analisis.R
\name{animar_historial_hca}
\alias{animar_historial_hca}
\title{Anima la evolución completa de la colonia a partir del \code{historial}
generado por \code{simular_hca()}, coloreando por resistencia (o nutriente).
Mismo lenguaje visual que el script de referencia (geom_point +
scale_colour_viridis_c("plasma") + coord_fixed + theme_minimal +
título "Paso: {n}" tipo frame_time), pero renderizado cuadro-a-cuadro
con magick/av en lugar de gganimate::animate().}
\usage{
animar_historial_hca(
  historial,
  archivo_gif = "figuras/evolucion.gif",
  archivo_mp4 = "figuras/evolucion.mp4",
  variable = c("resist", "N"),
  fps = 10,
  width = 600,
  height = 600,
  point_size = 0.6,
  alpha = 0.7,
  mostrar_fractal = TRUE,
  tamanos_fractal = c(2, 4, 5, 8, 10, 16, 20)
)
}
\arguments{
\item{historial}{lista de snapshots: cada elemento con
\code{paso}, \code{estado}, \code{N}, \code{resist} (tal como los guarda \code{simular_hca()})}

\item{archivo_gif}{ruta de salida .gif (NULL para omitir)}

\item{archivo_mp4}{ruta de salida .mp4 (NULL para omitir)}

\item{variable}{"resist" o "N": qué variable colorear}

\item{fps}{cuadros por segundo}

\item{width, height}{dimensiones en píxeles de la animación}

\item{point_size, alpha}{estética de los puntos}
}
\value{
invisible(NULL)
}
\description{
Nota de implementación: en este entorno no hay acceso a CRAN, así que
\code{gifski} (el renderer estándar de gganimate) no está disponible, y la
versión de \code{magick} instalada aquí es incompatible con
\code{gganimate::magick_renderer()} (bug conocido de compatibilidad de
versiones). Por eso se genera cada cuadro como PNG independiente con
ggplot2 y se ensamblan directamente con \code{magick::image_animate()}
(GIF) y \code{av::av_encode_video()} (MP4) — mismo resultado visual, sin
depender de esas piezas.
}
