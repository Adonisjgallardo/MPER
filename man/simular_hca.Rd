% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_motor.R
\name{simular_hca}
\alias{simular_hca}
\title{Ejecuta la simulación HCA de rescate evolutivo completa}
\usage{
simular_hca(
  nx = 100,
  ny = 100,
  n_steps = 600,
  D = 0.2,
  k = 0.15,
  dt = 1,
  N0 = 1,
  N_umbral = 0.15,
  p_max = 0.6,
  N_half = 0.3,
  g_inicial = 0.1,
  sigma_e = 0.05,
  mutacion_sd = 0.02,
  paso_introduccion = 150,
  tipo_plantilla = c("lineal", "uniforme", "radial"),
  modo_temporal = c("constante", "sinusoidal", "pulsos"),
  periodo_temporal = 40,
  A_max = 1,
  multiresistance = 0L,
  factor_dosis = 2,
  factor_exigencia = 1.5,
  tope_espera_shock = 150L,
  D_A = 0.15,
  delta_A = 0.02,
  tasa_dosificacion = 0.3,
  theta0 = 0.1,
  theta1 = 0.8,
  omega2 = 0.05,
  K_theta = 0.3,
  mort_base = 0.01,
  mort_estres = 0.9,
  K_mort = 0.3,
  Nc = 200,
  guardar_cada = 20,
  guardar_historial_en_disco = FALSE,
  historial_dir = NULL,
  seed = 123,
  on_step = NULL
)
}
\arguments{
\item{nx, ny}{dimensiones de la retícula}

\item{n_steps}{número de pasos de tiempo}

\item{D, k, N0, N_umbral, p_max, N_half}{igual que en la versión nutriente-pura}

\item{g_inicial, sigma_e}{parámetros de la colonia fundadora (genética cuantitativa)}

\item{mutacion_sd}{sd de la mutación aditiva de \code{g} por división}

\item{paso_introduccion}{paso en el que ocurre el CAMBIO AMBIENTAL ABRUPTO
(introducción del antibiótico). Antes de este paso, A=0 en toda
la retícula: la población crece libre (fase "silvestre").}

\item{tipo_plantilla}{heterogeneidad ESPACIAL del antibiótico:
"uniforme", "lineal" (gradiente) o "radial"}

\item{modo_temporal}{heterogeneidad TEMPORAL post-introducción:
"constante", "pulsos" o "sinusoidal"}

\item{periodo_temporal}{periodo (en pasos) de la fluctuación temporal}

\item{A_max}{concentración máxima de antibiótico post-introducción}

\item{multiresistance}{número de eventos de antibiótico ADICIONALES al
choque inicial (rango típico 0-10). Cada evento posterior introduce
un régimen más fuerte y más exigente según \code{factor_dosis} y
\code{factor_exigencia}; se disparan cuando la población recupera su
pico previo o al vencer \code{tope_espera_shock}, lo que ocurra primero}

\item{factor_dosis}{multiplicador geométrico de concentración (\code{A_max})
aplicado en cada evento adicional (1 = misma dosis siempre)}

\item{factor_exigencia}{endurecimiento geométrico de la selección en cada
evento adicional: omega2 se divide entre él, theta1 se empuja hacia
1 y mort_estres crece (1 = sin endurecimiento)}

\item{tope_espera_shock}{pasos máximos de espera desde el último evento
para que la población recupere su pico previo antes de disparar el
siguiente evento de todos modos}

\item{D_A, delta_A, tasa_dosificacion}{parámetros de la PDE del antibiótico}

\item{theta0, theta1, omega2, K_theta}{parámetros de selección estabilizadora (Caja 3)}

\item{mort_base, mort_estres, K_mort}{parámetros de mortalidad}

\item{Nc}{tamaño poblacional crítico para el análisis de tiempos de
persistencia (Caja 4 / Suplemento S2)}

\item{guardar_cada}{cada cuántos pasos se guarda un snapshot completo}

\item{seed}{semilla}

\item{on_step}{callback opcional \verb{function(paso, n_steps)} invocada al
final de cada paso de tiempo (util para barras de progreso en la
app Shiny); se ignora si es NULL}
}
\value{
lista: \code{historial} (snapshots), \code{resumen} (data.frame por paso,
incluye g_bar, var_g, z_bar, A_medio, nacimientos, muertes y
nivel_shock = eventos antibióticos ocurridos hasta ese paso),
\code{eventos_shock} (data.frame con paso/nivel/parámetros efectivos/
tipo de disparo de cada evento), estado final de todos los campos,
y \code{paso_extincion} (o NA)
}
\description{
Ejecuta la simulación HCA de rescate evolutivo completa
}
