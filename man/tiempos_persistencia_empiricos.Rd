% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_genetica_cuantitativa.R
\name{tiempos_persistencia_empiricos}
\alias{tiempos_persistencia_empiricos}
\title{A partir de una lista de resúmenes (de \code{simular_hca_replicas()}),
extrae el tiempo (en pasos DESDE la introducción del antibiótico)
en que cada réplica cruza por primera vez el umbral crítico Nc.
Réplicas que nunca cruzan el umbral devuelven \code{Inf} (censura por
la derecha: "sobrevivió todo el horizonte simulado").}
\usage{
tiempos_persistencia_empiricos(replicas, Nc = NULL)
}
\arguments{
\item{replicas}{lista devuelta por \code{simular_hca_replicas()}}

\item{Nc}{umbral crítico (si es NULL, usa el guardado en cada corrida)}
}
\value{
data.frame con columnas replica, tiempo_persistencia, extinguio
}
\description{
A partir de una lista de resúmenes (de \code{simular_hca_replicas()}),
extrae el tiempo (en pasos DESDE la introducción del antibiótico)
en que cada réplica cruza por primera vez el umbral crítico Nc.
Réplicas que nunca cruzan el umbral devuelven \code{Inf} (censura por
la derecha: "sobrevivió todo el horizonte simulado").
}
